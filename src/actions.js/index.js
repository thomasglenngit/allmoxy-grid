import movies 
import {
  FETCH_MOVIE,
  CREATE_MOVIE,
  EDIT_MOVIE,
  DELETE_MOVIE,
} from './types'

export const fetchMovie = (getState) => {

}

export const createMovie = formValues => async (dispatch, getState ) => {
  
  
}

export const editMovie = formValues => (dispatch, getState ) => {


}

export const deleteMovie = formValues => (dispatch, getState) => {


}